# Unreal Engine generated output
Binaries/
DerivedDataCache/
Intermediate/
Saved/
Build/Receipts/

# IDE and local tooling
.vs/
.vscode/
*.sln
*.suo
*.user
*.userprefs
*.VC.db
*.VC.opendb

# Local build and packaging output
ArchivedBuilds/
PackagedBuilds/

# Operating system files
.DS_Store
Thumbs.db

# Secrets and local platform configuration
Config/DefaultEngine.local.ini
Config/SteamDevAppId.txt
steam_appid.txt

# Fab / Marketplace packs are re-downloadable from the Fab library and must NOT
# go into Git LFS: Vehicle Variety Pack alone is 1.4 GB against a 1 GB free quota,
# and the code degrades gracefully without it (the FObjectFinders return null and
# every profile falls back to the previous SportsCar assembly). See ENVIRONMENT.md
# for how to re-fetch it.
/Content/VehicleVarietyPack/

# ambientCG source JPGs. The imported .uassets in Content/Environment/Textures are
# what the game uses; the 195 MB of loose source images are only needed to re-import,
# and would otherwise go through Git LFS for no benefit.
/Content/Environment/Source/
